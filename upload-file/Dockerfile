
FROM node:slim
WORKDIR /usr/src/app
COPY package*.json ./
RUN npm install
COPY . .
ENV MINIO_USER=${MINIO_USER}
ENV MINIO_PASSWORD=${MINIO_PASSWORD}
ENV MINIO_PORT=${MINIO_PORT}
ENV HOST=${MINIO_HOST}

EXPOSE 3000
CMD [ "node", "index.mjs" ]